{
  "name": "radar-security-tests",
  "private": true,
  "engines": { "node": ">=24" },
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/*.test.cjs",
    "test:rules": "firebase emulators:exec --project demo-radar-security --only firestore 'npm test'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.30.2"
  }
}
